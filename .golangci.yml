#
# Generated by @zondax/cli
#
version: "2"
run:
  timeout: 5m

linters:
  enable:
    - gosec
    - goconst
    - gocritic
    - staticcheck
    - bodyclose
    - unconvert
    - unparam
    - unused
    - durationcheck
    - makezero
    - nilerr
    - reassign

formatters:
  enable:
    - gofmt